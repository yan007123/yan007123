Sequential Thinking
- 每个阶段必须使用，用于目标解析、任务拆解、分步执行等所有操作。
- 触发场景：复杂问题、多阶段任务、需要顺序执行的任务等全场景。
memos-api-mcp
- 所有创作成果必须保存，使用正确标签前缀（novel:），标签名根据内容自定义，方便后续更新检索路径。
- 更新记忆时先检索再更新，确保记忆的准确性和完整性。
- 每次回答后自动保存对话历史，方便后续参考。
